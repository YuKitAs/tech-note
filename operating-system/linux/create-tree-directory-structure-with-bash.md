# Create Tree Directory Structure with Bash

The script `mkdir -p src/{main/{java,resources},test/{java,resources}}` can be used to create directories with the following structure at once:

```
├── main
│   ├── java
│   └── resources
└── test
    ├── java
    └── resources
```
